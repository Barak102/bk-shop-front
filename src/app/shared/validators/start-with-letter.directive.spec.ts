import { StartWithLetterDirective } from './start-with-letter.directive';

describe('StartWithLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new StartWithLetterDirective();
    expect(directive).toBeTruthy();
  });
});
